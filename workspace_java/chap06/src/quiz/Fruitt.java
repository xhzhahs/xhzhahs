package quiz;

public class Fruitt {

	// 과일마다 이름, 가격, 재고를 배열로 만들려고 했는데 
	// String, int가 섞여 타입이 달라서 클래스로 만드는 것이다.
	// ex) {"토마토", 1000, 5} 이런식으로는 배열을 만들 수 없기 때문이다.
	// 배열은 타입이 같은 것들만 넣을 수 있다.
	
	// 이력서 양식같은 존재
	// 이름 쓰는 칸은 있지만 이름이 쓰여져 있지는 않다.
	String name; 	// 이름
	int price;   	// 가격
	int stock;  	// 재고
	
	
	
	
	
	
}
